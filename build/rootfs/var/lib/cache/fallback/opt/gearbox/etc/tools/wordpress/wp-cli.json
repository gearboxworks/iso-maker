{
    "type": "container",
    "description": "The WordPress CLI command line tool",
    "network": "box",
    "restart": "no",
    "volumes": [
        { "host": "${BOX_PROJECTS_ROOT}/${BOX_PROJECT_PATH}/www", "container": "/var/www", "mount": true }
    ]
}
